<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="at.ac.tuwien.ims.lifestage.androidtounityinterface">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <receiver android:name=".AndroidToUnityReceiver" >
            <intent-filter>
                <action android:name="orientationTracker_update" />
            </intent-filter>
            <intent-filter>
                <action android:name="at.ac.tuwien.ims.lifestage.vibro.button" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
